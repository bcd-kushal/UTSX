import { Position } from "../utils/types"

export const SLNO_COL_WIDTH = "50px"


export const vertical: Position = {
    top: "flex-start",
    center: "center",
    bottom: "flex-end"
}

export const horizontal: Position = {
    left: "flex-start",
    center: "center",
    right: "flex-end"
}
